<h2>All Users</h2>
<%= link_to 'Create User', signup_path %> | <%= link_to 'Add Roles', new_roles_path %>
<table>
  <tr>
    <th>Username</th>
    <th>Email</th>
    <th>Roles</th>
    <th></th>
  </tr>
    <%= render :partial => 'user', :collection => @users %>
</table>